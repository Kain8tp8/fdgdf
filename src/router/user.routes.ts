import {Router} from 'express';
import userService from '../service/user.service';
import User from '../model/user.model'

const router = Router()

router.get('/', (req,res)=>{

})

router.post('/login', (req,res)=>{

})

router.delete('/remout', (req,res)=>{

})




export default router









